# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

# The next line updates PATH for the Google Cloud SDK.
#if [ -f '/home/gwendt/google-cloud-sdk/path.bash.inc' ]; then . '/home/gwendt/google-cloud-sdk/path.bash.inc'; fi

# The next line enables shell command completion for gcloud.
#if [ -f '/home/gwendt/google-cloud-sdk/completion.bash.inc' ]; then . '/home/gwendt/google-cloud-sdk/completion.bash.inc'; fi
